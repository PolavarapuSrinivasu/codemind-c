#include<stdio.h>
int main()
{
    int n;
    scanf("%d",&n);
    int i,a,b,j;
    for(j=0;j<n;j++)
    {
        scanf("%d%d",&a,&b);
        int c=0;
    for(i=a;i<=b;i++)
    {
        if(i%10==2 || i%10==3 || i%10==9)
        {
            c+=1;
        }
    }
    printf("%d\n",c);
    }
}
