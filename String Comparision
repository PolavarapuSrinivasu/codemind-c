#include<stdio.h>
#include<string.h>
int main()
{
	char str1[100],str2[100];
	scanf("%[^\n]s",str1);
	scanf(" %[^\n]s",str2);
	int s=strcmp(str1,str2);
	if(s==0)
	{
	    printf("Both are same");
	}
	else
	{
		printf("Both are not same");
	}
	else if(s<0)
	{
		printf("str1<str2");
	}
	else
	{
		printf("str1>str2");
	}
}
