#include<stdio.h>
int main()
{
	int size,i;
	scanf("%d",&size);
	int a[size];
	for(i=0;i<size;i++)
	{
		scanf("%d",&a[i]);
	}
	for(i=0;i<size;i++)
	{
		printf("index:a[%d]---->value:%d\n",i,a[i]);
	}
}
