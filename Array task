#include<stdio.h>
int main()
{
	int a[10],n,i,j,s;
	scanf("%d",&n);
	for(i=0;i<n;i++)
	{
		scanf("%d",&a[i]);
	}
	for(i=0;i<n;i++)
    {
 	   s=0;
	   for(j=0;j<n;j++)
	   {
		if(a[i]==a[j])
		{
			s++;
		}
	   }  
	   printf("%d --> %d\n",a[i],s);
    }
}
